<div class="challenge-wrapper">
  {{ content_for_layout }}  {% comment %} <--- conent_for_layout IS the captcha form in this context {% endcomment %}
  <style>
    /* NOTE: these classes are subject to change at any time... since they are injected by shopify / google captcha */
    .challenge-wrapper {
      padding-top: calc(var(--headerHeight--mobile) + var(--spacing-mobile));
    }
    @media only screen and (min-width: 768) {
      .challenge-wrapper {
        padding-top: calc(var(--headerHeight--mobile) + var(--spacing-tablet));
      }
    }
    @media only screen and (min-width: 1280px) {
      .challenge-wrapper {
        padding-top: calc(var(--headerHeight--mobile) + var(--spacing-desktop));
      }
    }
  </style>
</div>